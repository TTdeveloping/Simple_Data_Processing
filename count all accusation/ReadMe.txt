这是一个统计每个罪名包括多少数据的程序：

accu.txt是202条罪名的名称

All_legal_data.json要被统计的文件，数据格式是json格式

count.txt 是运行程序之后输出的结果，记录着每条罪名以及其对应的条数

count_data_handel.py 是运行代码



This is a program that counts how much data is included for each crime:

accu.txt is the name of 202 counts

All_legal_data.json file to be counted, the data format is json format

count.txt is the result of the output after running the program, recording each crime and its corresponding number

count_data_handel.py is the run code

